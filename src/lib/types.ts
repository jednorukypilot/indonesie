export type Section = {
    name: string;
    name_short: string;
    color: string;
    styleId: string;
    url: string;
    note: string;
};

export type Device = {
    name: string;
    url: string;
    note: string;
};